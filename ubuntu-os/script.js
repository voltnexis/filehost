document.addEventListener('DOMContentLoaded', function() {
    // No complex JavaScript needed since the download is a redirect link.
    // The link itself handles the action: a href="https://voltnexis.github.io/os"
});
